# 2026-07-31 (continuing Cover Nugget)

## SERIF FONT — actually fixed (root cause found)
- RN 0.81 `Text` is a plain function component (`const TextImpl: component(...)`), NO `.render` → my earlier monkey-patch silently no-op'd (guard returned). THAT'S why serif never showed.
- Fix: `src/ui/serif.tsx` wraps Text/TextInput, injects `{fontFamily: Georgia(iOS)/serif(Android)}` as first style, registers `cssInterop(...,{className:"style"})` so className still works. Script-swapped Text/TextInput imports from "react-native" → "../ui/serif" across 18 files. Removed the dead render-patch from app/_layout.tsx. tsc + ios/web export green.

## Other UI (2026-07-31)
- Settings: deleted the "Choose Light/Dark/Auto" subtitle; **AppearanceToggle now slideable** (animated highlight slides to selection via onLayout width + Animated.spring translateX).
- **GPA** added to education: schema col + migration (SCHEMA_VERSION 3→4), model/repo (map/add/update), profile Education field + onboarding education field.
- **Unified Save buttons:** new SaveButton (says "Save", shows "✓ Saved" 1.8s after success; returns false = no tick). Used in profile Personal/Skills/Additional + RepeatableEditor. Dropped the "Saved" alerts.
- **Profile editable no more empty gap:** RepeatableEditor editable mode now always shows ≥1 form (blank temp entry when empty); temp entries (negative id) persist on Save via add(); existing via update(); remove re-seeds a blank if empty.
- AI copy toning: earlier turn (model→"Your Assistant", etc.).
- Verified tsc + ios & web export exit 0. **User must `npm install`? NO — no new deps.** cnpull + full restart recommended (serif import swap + _layout change).

## Jobs backend (JobSpy) — Oracle VM chosen (2026-07-31)
- User picked Oracle Always-Free VM for the job-scraper backend.
- Built `side-project/server/`: FastAPI `main.py` (/health, /fetch via trafilatura server-side extraction, /search via JobSpy), `requirements.txt`, `DEPLOY.md` (Oracle Ampere A1 VM + systemd + Cloudflare Tunnel for free HTTPS since iOS ATS requires https).
- App wiring: `src/job/jina.ts` now tries `EXPO_PUBLIC_JOBS_API/fetch?url=` FIRST (server-side, better for company/ATS pages), falls back to Jina/paste. Blocked → clear error. Set EXPO_PUBLIC_JOBS_API in .env on the Mac to the tunnel URL.
- Also this turn: 📄 icon + bigger Generate button; hamburger reverted to ☰ (removed emoji headerLeft); archive search bar (local title+content filter, no backend). All tsc+ios export green.
- STILL PENDING user action: provision Oracle VM + deploy (their account; I gave the guide). Indeed/LinkedIn still blocked on datacenter IPs (need paid proxies) — company/ATS pages work.
- Reminders: no new npm deps → cnpull + reload for the app changes.

## Fixes (2026-07-31): header/drawer serif, revert appearance, export layout
- Header title + drawer labels were React-Navigation-rendered (not our serif wrapper) → set headerTitleStyle.fontFamily + drawerLabelStyle.fontFamily = SERIF in (app)/_layout screenOptions. (DrawerContent "Cover Nugget" was already serif.)
- Reverted AppearanceToggle to the simple tap-segmented control (slideable version was "ruined"); removed now-unused Animated/useEffect/useRef imports from settings.
- Export screen rebuilt: ScreenContainer scroll={false}; full letter in a flex-1 scrollable box; actions (Copy/PDF/Word/Google Docs/Share) as a 2-per-row wrap grid at the bottom (Copy=primary, rest=secondary green), Done below. No more 6-line truncation.
- tsc + ios export green. No new deps → cnpull + reload.

## Back buttons + editor top + profile card format (2026-07-31)
- BackButton: now a round "←" in a pink (bg-highlight) circle, like the sidebar highlight. No text label. Used in generate/export.
- Editor top bar restructured: round back button + Edit/Done on row 1; editable title moved to its own row below (text-xl) so nothing's cramped.
- Profile tabs consistency: wrapped Personal / Skills / Additional content in a Card (+ SaveButton below) to match the Edu/RepeatableEditor card format. All tabs now look the same.
- Also: job desc placeholder → "Paste the job description here".
- tsc + ios export green. No new deps → cnpull + reload.

## On-device scraper (no backend!) (2026-07-31)
- User asked for a no-server scraper better than Jina for jobs. KEY INSIGHT: RN fetch has no CORS + uses the PHONE's residential/cellular IP → far less bot-blocked than datacenter readers (Jina/Oracle/HF).
- src/job/jina.ts: added fetchDirect(url) — fetches the page on-device with a mobile Safari UA, extracts the **JobPosting JSON-LD `description`** (Greenhouse/Lever/LinkedIn-guest/most boards embed it) via extractJobPosting + htmlToText. fetchJobTextFromUrl now tries: (1) on-device direct JSON-LD, (2) optional backend, (3) Jina, else paste.
- No backend/key/account needed. Oracle/HF backend is now OPTIONAL (kept as fallback). This likely removes the need for the whole VM/Space saga.
- Raw full-page strip intentionally NOT returned (too noisy) — only clean JSON-LD; defers to Jina otherwise.
- tsc + ios export green. No new deps → cnpull + reload. Test with a Greenhouse/Lever link.

## Scraper polish (2026-07-31): sites message, length cap, bar animation
- On-device scraper confirmed working on Greenhouse. Added generate-screen note: works on company pages/Greenhouse/Lever/Ashby; Indeed & LinkedIn blocked → paste.
- **14k-char scrape too big for 0.5B model:** prompt.ts capDescription() truncates job description to 4000 chars for the model prompt (postings front-load key info). Bumped llama.rn n_ctx 2048→4096 in registerNative.native.ts. (Template path unaffected; full text still stored on the letter.)
- **Loading bar didn't animate:** NativeWind className on the Animated.View was clobbering the native transform. Rewrote ScrapeBar with inline styles + onLayout width + useApp colors; segment slides across. (Also note: on-device fetch is fast so the bar may only show briefly.)
- tsc + ios export green. No new deps → cnpull + reload.
