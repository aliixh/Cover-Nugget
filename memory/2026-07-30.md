# 2026-07-30

- Created `side-project/` as the dedicated, isolated home for this project.
- Per user: build a brand-new folder for the project; do NOT interact with any existing workspace files.
- Replicated the memory file structure here (MEMORY.md + memory/) so the project is fully self-contained.
- Still open: a name/vibe/emoji for me, user's preferred name.

## Cover Nugget — Phase 1 built (2026-07-30)
- User handed the full spec for **Cover Nugget** (privacy-focused local-first AI cover letter app, RN + Expo + TS + NativeWind + expo-sqlite). Saved to MEMORY.md.
- Hard constraint reaffirmed: **do NOT use the GPU** ("no interrupting GPU, only your cloud"). All work CPU/cloud only. Local Qwen model stays a Phase-4 stub.
- Built entire **Phase 1** inside `side-project/`: Expo config, NativeWind theme, SQLite schema + repositories, AppContext (DB init + theme + onboarding gate), reusable components, 9-step onboarding, drawer nav, Main/Archive/Profile/Settings screens, and a modular AI seam (`src/ai/`) with a NotImplementedProvider + pure prompt builders.
- Fixed a route collision (app/index.tsx vs (app)/index.tsx both = "/"); renamed main screen to `/home`.
- **Did NOT run `npm install`** (heavy) — scaffold is structurally verified but not yet typechecked/built. Next: `cd side-project && npm install && npm run typecheck`, then `npx expo start`.
- Next phases: P2 job input + editor + export; P3 wire AI gen + highlight edits; P4 Jina Reader + local Qwen + AdMob + release.

## Cover Nugget — auto-build + model download (2026-07-30)
- Ran `npm install` (1241 pkgs) — CPU/network only, **no GPU**. App builds: `tsc --noEmit` passes and `expo export --platform web` bundles clean (1007 modules).
- Added web renderer deps (react-dom, react-native-web, @expo/metro-runtime).
- **Build fix:** NativeWind 4.2.x hard-codes the reanimated-4 `react-native-worklets/plugin`, which breaks SDK 51 (reanimated 3). Pinned `nativewind` to **4.1.23** (css-interop 0.1.22) → fixed.
- **On-device model download built** (spec §16, Phase-4 groundwork): `src/ai/modelConfig.ts` (GGUF URL — default points at a small Qwen2.5-0.5B-Instruct Q4_K_M; confirm exact weight), `modelManager.ts` (expo-file-system download w/ progress, status, delete), `runtime.ts` (LlamaRuntime seam), `localProvider.ts` (real prompts + runtime). New **AI Model** drawer screen + Home first-run download banner.
- **Key architecture note:** model *download* works in Expo Go/web; model *inference* needs a **Dev Client build** (llama.rn is a native module Expo Go can't load). Documented in `docs/AI_MODEL.md`. App bundles everywhere because we never statically import llama.rn — a dev build registers it via `setLlamaRuntime()`.
- Run instructions written to `RUN.md` (web / emulator / iPhone via Expo Go / dev-client for LLM).

## Cover Nugget — auto model download on first run (2026-07-30)
- User asked if the LLM can auto-download when they get the app. Implemented the practical version: **`app/model-setup.tsx`** auto-starts the download on first launch (no tap), progress bar, resume, "Skip for now". Gate (`app/index.tsx`) routes onboarded native users → model-setup until a `model_setup_done` meta flag is set (download complete or skipped); web skips it. Onboarding finish now routes through `/` so the gate decides.
- Added `getMeta`/`setMeta` (meta table) to repositories.
- True "ships with the store install" = Android Play Asset Delivery (fast-follow) / iOS On-Demand Resources — noted as Phase-4 native work, not done yet.
- Re-verified: `tsc` clean + web export builds.

## Cover Nugget — Phases 2–4 built (2026-07-30)
- **Phase 2:** `app/generate.tsx` (job link via Jina Reader OR pasted description), `app/editor/[id].tsx` (full manual editing, highlight AI toolbar, whole-letter rewrites, custom-edit modal, save/finish), `app/export/[id].tsx` (copy / PDF via expo-print / Word via HTML .doc / Google Docs via docs.new + clipboard / share via expo-sharing). Wired Home→generate, Home/Archive→editor.
- **Phase 3:** generation + edits go through `src/services/coverLetter.ts` → `getAI()`. **Local template fallback** (`src/ai/template.ts`) so the whole flow works in Expo Go/web with NO model. AI settings (permanent instructions) are folded into every prompt.
- **Phase 4 groundwork:** Jina Reader (`src/job/jina.ts`), modular **AdMob seam** (`src/ads/ads.ts` + `AdBanner` on Home, no-op until a native build registers it), `eas.json`, `docs/RELEASE.md`, `docs/MONETIZATION.md`. Real LLM inference + ads still need a Dev Client build (native modules can't load in Expo Go) — documented.
- Added deps: expo-clipboard, expo-print, expo-sharing.
- Verified again: `tsc --noEmit` clean + `expo export --platform web` builds.
- Packaged `../cover-nugget.tar.gz` (178K, no node_modules) for the user to download to their Mac.
- **Still TODO for real ship:** register llama.rn runtime + google-mobile-ads in a dev build, supply real Qwen GGUF URL, app icon/splash, EAS build+submit. Optional: Play Asset Delivery / iOS ODR to bundle model into the install.

## Cover Nugget — app assets + model URL verified (2026-07-30, 05:33 UTC)
- **App icon/splash generated** (were missing = ship blocker). Authored dino-nugget mascot SVG (golden nugget, sage dino back-plates, pink cheeks, deep-green field per brand palette) and rasterized with sharp (CPU, no GPU) to `assets/`: icon.png (1024, full-bleed), adaptive-icon.png (1024, transparent 68% safe zone), splash-icon.png (1024), favicon.png (48). Source kept as `assets/mascot.svg`; regen via `node assets/gen-icons.mjs`. sharp installed --no-save at workspace root, NOT in app package.json.
- Wired app.json: expo.icon, splash.image, android.adaptiveIcon (bg #12372A), web.favicon.
- **Verified Qwen GGUF URL is live** — resolves to a 491,400,032-byte file (~469 MB). Updated modelConfig.ts approxSizeMB 400→469 + note.
- Re-verified: tsc --noEmit clean + expo export --platform web builds clean (favicon picked up).
- **Remaining = Mac-only** (can't do from cloud): Dev Client build registering llama.rn + google-mobile-ads, EAS build+submit. Everything buildable from here for a v1 is now done.

## Cover Nugget — Phase 4 code-complete: native seams WIRED (2026-07-30, 05:40 UTC)
- User: "just finish the app entirely." The real unfinished part wasn't a phase — the native registration was only *documented* (manual paste in AI_MODEL.md), never written, and native deps weren't declared. Fixed:
- **Wrote the registration** as a platform split so web/Expo Go stay green AND a dev build auto-wires with zero further code:
  - `src/native/registerNative.ts` — web/default no-op stub.
  - `src/native/registerNative.native.ts` — real: `require('llama.rn')` → setLlamaRuntime; `require('react-native-google-mobile-ads')` → setAdsRenderer (TestIds unless EXPO_PUBLIC_ADMOB_BANNER_ID). Gated on `Constants.executionEnvironment==='storeClient'` so Expo Go stays download-only; try/catch so missing modules degrade to template/no-ads.
  - `app/_layout.tsx` calls `registerNative()` once at module load.
- **Declared native deps** in package.json (NOT installed here — keeps web export green; user installs on Mac): `expo-dev-client ~4.0.29`, `llama.rn ^0.4.6`, `react-native-google-mobile-ads ^13.6.0`.
- Updated docs to reflect wiring is automatic: AI_MODEL.md, MONETIZATION.md, RELEASE.md; README Phase 4 → ✅.
- Verified: `tsc --noEmit` clean + `expo export --platform web` OK after every change.
- **Repackaged `../cover-nugget.tar.gz` (316K)** with new assets + native wiring.
- **Truly remaining = NOT code (needs user's Mac + accounts):** `npm install` + `eas build --profile development/production` + `eas submit`; and pasting their AdMob app ids into app.json (`react-native-google-mobile-ads` plugin) — can't invent account ids. Optional: real Qwen3.5-0.8B GGUF swap (current default Qwen2.5-0.5B verified live), Play Asset Delivery / iOS ODR to bundle model into install.

## Cover Nugget — AdMob held off, LLM ships (2026-07-30, 17:21 UTC)
- User: hold off AdMob but keep space. Removed `react-native-google-mobile-ads` from package.json and removed its `require`/registration from registerNative.native.ts (left a clearly-marked re-enable block + kept the ads.ts seam + <AdBanner/>, which render nothing). Reason: the SDK auto-inits on launch and crashes without an AdMob App ID, and Metro would try to resolve the require in the native bundle. LLM (llama.rn) + expo-dev-client stay wired.
- Verified tsc + web export green; repackaged cover-nugget.tar.gz (317K).
- Gave user exact npm install + eas build (dev + production) + eas submit steps.

## Cover Nugget — upgraded SDK 51 → 54 (2026-07-30, 20:11 UTC)
- Reason: user reinstalled Expo Go from App Store → got latest (SDK 54 only). Modern Expo Go supports only the latest SDK; SDK 51 project was incompatible ("need SDK 51, is 54"). iOS can't install old Expo Go, and a dev-client build needs a paid Apple acct — so Expo Go (free) forced the upgrade. SDK 54 is also the better ship target anyway.
- Did full migration on the A100 (CPU only, no GPU): expo ^54.0.36, react 19.1.0, react-native 0.81.5, expo-router ~6, @react-navigation/drawer ^7.5.0, reanimated ~4.1.1, all expo-* bumped via `expo install --fix`.
- Fixes required by the jump:
  - Added `react-native-worklets` (0.5.1) + switched babel plugin `react-native-reanimated/plugin` → `react-native-worklets/plugin` (reanimated 4).
  - `@react-navigation/drawer` 6→7 (expo-router 6 peer).
  - `@types/react` →19, typescript →5.9.
  - Added `babel-preset-expo` as explicit dep (no longer transitive in SDK 54).
  - **expo-file-system split:** classic API (documentDirectory/EncodingType/download resumable) moved to `expo-file-system/legacy` — updated imports in src/ai/modelManager.ts + src/services/export.ts.
  - **metro.config.js:** added `wasm` to resolver.assetExts (SDK 54 expo-sqlite ships a web wasm backend; note: expo-sqlite now HAS web support, so the earlier "sqlite crashes on web" limitation is gone).
  - nativewind kept at 4.1.23 (worked with the new stack; didn't need bumping).
- Verified: clean `rm -rf node_modules && npm install` exit 0 (no peer conflicts), tsc clean, `expo export --platform web` AND `--platform ios` both exit 0 (iOS = the bundle Expo Go runs). Could NOT run in Expo Go from cloud — runtime on-device still unverified by me.
- Repackaged cover-nugget.tar.gz (273K). User must re-pull via scp + fresh `npm install`.

## Delivery preference (2026-07-30, 20:15 UTC)
- **User prefers in-place updates over remove-and-redownload** when syncing code to their Mac. Going forward hand them an `rsync -az --delete --exclude node_modules --exclude .expo --exclude dist ubuntu@161.153.75.241:/home/ubuntu/.openclaw/workspace-new-project/side-project/ ~/Downloads/side-project/` command (updates changed files, preserves node_modules) instead of `rm -rf` + scp + tar. Only tell them to `npm install` when deps actually changed. Offered git-repo + `git pull` as an even cleaner option (pending user yes).

## Cover Nugget — onboarding/profile UX fixes (2026-07-30, 20:xx UTC)
- App runs on user's iPhone via Expo Go (tunnel + --go). User gave product feedback; implemented:
  1. **Work + Volunteer combined** into ONE onboarding step (app/onboarding/experience.tsx now renders two RepeatableEditors). Deleted volunteer.tsx. Renumbered steps 9→8 total across personal(2)/education(3)/experience(4)/projects(5)/skills(6)/certifications(7)/additional(8).
  2. **"Add doesn't work / loses input" fixed:** new `src/components/RepeatableEditor.tsx` (forwardRef, exposes flush()). Drafts auto-save on Continue/Skip/Back (RepeatableSection + combined page await ref.flush() before navigating) so typed-but-not-"+Added" entries are never lost. Added "Added (N)" list header + "+ Add another" label for clarity.
  3. **Everything editable in Profile:** collection sections (education/experience/volunteer/projects/certifications) were READ-ONLY — now use RepeatableEditor editable=true with inline per-field editing that saves on change (fire-and-forget update). Added updateEducation/Experience/Volunteer/Project/Certification to repositories.ts.
  4. **AI setup copy** (app/model-setup.tsx): "Hold on tight!" + "one-time download" + "100% free forever" + offline/private.
- No new deps → user does NOT re-run npm install; just rsync + restart expo.
- Verified: tsc clean + expo export ios & web both exit 0. NOT tested on device by me (user will).
- Note: inline edit writes to SQLite per keystroke (fire-and-forget). Fine for now; debounce if laggy.

## Cover Nugget — job-info + editor UX + LLM clarity (2026-07-30, 20:xx UTC)
- User confused why letters ignore the job link and why AI edits say "unavailable". Root causes found + explained:
  - **The LLM does NOT run in Expo Go.** Letters come from the TEMPLATE fallback (src/ai/template.ts), not Qwen. llama.rn is a native module Expo Go can't load. Real AI needs a dev/prod build. iOS dev build needs paid Apple Developer acct ($99/yr); **Android dev build is free** (sideload APK) — told user this is the way to actually run the AI without paying Apple.
  - **Template ignored the job description** entirely (only used company/role names) — that's the "only using my info" bug. The real-model prompt (prompt.ts buildGeneratePrompt) DOES use job.description, so only the fallback was broken.
- Fixes shipped (all verified tsc + ios export):
  1. **template.ts** now reads job.description: matches candidate skills against the posting, references the job's stated emphasis (firstRequirement), extracts role if not given. Tailored draft even without the model.
  2. **Editor rewritten (app/editor/[id].tsx):** opens in read-only PREVIEW with an "Edit" button on top; edit mode shows the editable text + always-visible button-driven "Edit selection" actions (highlight then tap, no reliance on the flaky selection popup) + whole-letter actions. Added "Done" to return to preview. Persists on mode switch.
  3. **Clearer AI-unavailable message:** "AI edit needs the full app … You can still edit by typing directly."
- AI edit buttons ("More professional"/"Shorter"/highlight) still can't run in Expo Go by design (need the model). Manual editing fully works.
- No new deps → user rsyncs + presses r.
- OPEN DECISION for user: to run the real AI, build Android dev client (free) vs pay Apple $99 for iOS. Awaiting choice.

## Cover Nugget — keyboard + letter naming/rename (2026-07-30, 20:xx UTC)
- **Keyboard trap fixed:** editor now has an iOS InputAccessoryView "Done" toolbar above the keyboard (Keyboard.dismiss) on the title/body/custom inputs, + wrapped edit area in KeyboardAvoidingView. Top "Done" button already returns to preview.
- **Cover-letter naming:** added `title` column to cover_letters (SCHEMA_VERSION 1→2) with an additive migration (ensureColumn via PRAGMA table_info + ALTER TABLE) so existing DBs upgrade without wipe. defaultLetterTitle() auto-names on generate ("<Company> — <Role>" / "<Company> Cover Letter"). NOTE: not truly "AI"-generated (LLM doesn't run in Expo Go) — deterministic from company/role; can swap to model-generated later.
- **Rename everywhere:** editor top title is now an editable TextInput (persists via updateCoverLetterTitle, fire-and-forget); archive has a Rename button → modal; home + archive show coverLetterTitle() (title with company/role fallback). Added updateCoverLetterTitle + coverLetterTitle() helper.
- Verified tsc + ios & web export all exit 0. No new deps → user rsync + press r.

## Cover Nugget — dark-mode contrast, theme toggle, relevance (2026-07-30, 20:xx UTC)
- **Root-cause dark-mode bug:** `text-muted` (and `border`/`dark-border`, `surface`) were NEVER defined in tailwind.config.js → those classes produced no color → RN default BLACK. Fine on light cream bg, invisible on dark green. FIX: added muted/dark-muted/border/dark-border/surface to tailwind theme, and sed-added `dark:text-dark-muted` to every `text-muted` usage across app/ + src/.
- **Light/Dark/Auto setting:** AppContext now persists `theme_pref` in meta + applies via nativewind setColorScheme on boot; added ThemePref type + setThemePref. Settings has an "Appearance" segmented control (Light/Dark/Auto). App was auto-following device (system) — now user-overridable.
- **"dino" relevance bug:** template.ts now only includes skills the JOB TEXT actually mentions (matchedSkills), not all profile skills — so joke/irrelevant skills don't bleed in. buildGeneratePrompt (real LLM) got an explicit "only include details RELEVANT to this role, no padding" instruction. (Reminder: letters in Expo Go still come from the template, not the LLM.)
- Verified tsc + ios & web export exit 0. No new deps → rsync + press r.

## Cover Nugget — serif font, skip-all, scraper preview, lighter onboarding (2026-07-30, 20:xx UTC)
- Confirmed to user: Android-first then Apple later is fine (one codebase, no iOS issues). Expo Go CANNOT run the real LLM — only a dev-client build (Android free / iOS paid) can. No Expo Go + real LLM path exists.
- **Global serif font:** app/_layout.tsx sets Text + TextInput default fontFamily via Platform.select({ios:"Georgia",android:"serif"}) — no bundled fonts, works in Expo Go, bold weights work. className still controls size/weight/color.
- **Skip all:** OnboardingScaffold now has a "Skip all" button (top-right by progress dots) → getOrCreateProfile + refreshOnboarded + replace("/"). Footer raised (mb-4).
- **Lighter onboarding (fewer boxes):** trimmed onboarding-only fields — education→3 (school/degree/gradYear), work→4 (drop achievements), volunteer→3 (drop dates), projects→3 (drop results). Full fields still editable in Profile (separate configs).
- **Scraper dev tool:** generate.tsx (link mode) has "🔍 Preview scraped text (dev)" → modal showing exactly what Jina Reader returned (char count + scrollable text), so user can verify scraping.
- Verified tsc + ios & web export exit 0. No new deps → rsync + press r.

## Cover Nugget — export naming + preview-first (2026-07-30, 20:xx UTC)
- **Export file naming:** export screen now has a "File name" field pre-filled from the letter title (company/role-based sample). On any export it (a) saves the name back as the letter title (→ archive uses it) and (b) names the actual file. export.ts: exportPdf/exportWord/shareText take fileName; added safeFileName() (strips path-illegal chars); PDF copied from expo-print's random name to <name>.pdf.
- **Archive/recent already open read-only:** editor defaults to preview mode (text + top Edit button). Added a **Copy** button to the preview bottom row (preview = Copy + Export; edit = Save + Export). So viewing a letter isn't immediately editable.
- Verified tsc + ios & web export exit 0. No new deps → rsync + press r.

## Cover Nugget — prep Android dev build (2026-07-30, 21:11 UTC)
- User wants the FREE Android dev build to finally run the real LLM. Confirmed: NO publishing/store needed — dev build is a private APK installed directly.
- **Bumped llama.rn 0.4.6 → ^0.12.8** (latest; 0.4.x too old for RN 0.81/new-arch → would fail the native build). Verified: npm install exit 0, tsc clean, web export OK, initLlama + completion API present (matches registerNative.native.ts wiring: initLlama({model,n_ctx}) + ctx.completion({prompt,n_predict}).text).
- **⚠️ This changed a dep → user MUST run `npm install` after syncing** (not just rsync+r).
- Next: user runs `eas build --profile development --platform android` (Expo cloud, ~15 min, no GPU/A100) → installs APK on Android device/emulator → `npx expo start --dev-client --tunnel` → first launch auto-downloads ~469MB Qwen → real on-device AI. eas.json development profile (developmentClient:true, distribution internal) already set.
- Risk: first native build may need a fix (llama.rn/newArch quirk); handle iteratively.

## CORRECTION — free iOS testing IS possible (2026-07-30, 21:46 UTC)
- I earlier said iPhone testing needs the $99 Apple Developer Program. WRONG / too absolute. A **free Apple ID → "Personal Team"** can sign + sideload a dev build onto the user's OWN connected iPhone via Xcode. The $99 is only for distribution to OTHERS (TestFlight / App Store / EAS *cloud* iOS builds).
- Free iOS route = LOCAL build (not EAS): install Xcode, connect iPhone via USB, add Apple ID in Xcode (Personal Team), then `npx expo run:ios --device` → builds+signs+installs a dev client with llama.rn (real LLM). Then `npx expo start --dev-client`.
- Free-team caveats: app expires after **7 days** (re-run to reinstall), ~3 apps max per free ID, phone must be plugged in. Needs Xcode (~7GB) which user had skipped.
- Status: Android emulator dev build already queued on EAS (build 6eb61405) as the no-Xcode path. User deciding Android emulator vs free iPhone/Xcode.

## Scraper: Indeed/LinkedIn are bot-walled (2026-07-30, 21:52 UTC)
- User tried an Indeed link → Jina returned a Cloudflare "Additional Verification Required" page, not the job. Indeed/LinkedIn block all free scrapers server-side; not fixable client-side.
- jina.ts hardened: X-Return-Format:text + X-Remove-Selector (nav/header/footer) for cleaner output; cleanJobText() strips md images/links/urls; looksBlocked() detects Cloudflare/login/captcha markers and throws a clear "site blocked automatic reading … Paste Description" error; min-length raised to 120. generate.tsx auto-switches to Paste mode on a link failure.
- Reliable path for Indeed/LinkedIn = paste the description. Scraper works better on ATS/company pages (Greenhouse/Lever/company career sites).
- Verified tsc + ios export. No new deps → cnpull + r.

## Cover Nugget — UI polish batch (2026-07-30, 22:07 UTC)
- **Back buttons:** new BackButton component (router.back); added to generate + export screens; back chevron in editor top bar (fixes "can't leave editor without exporting").
- **Profile → browser-style TABS** (Personal / Skills / Work·Volunteer / Education / Projects / Certifications / Additional) instead of one long scroll.
- **RepeatableEditor editable mode redesigned:** removed the confusing duplicate empty draft form; entries are inline-editable cards + a "+ Add <section>" button that inserts a blank editable entry. (Onboarding mode unchanged: summary cards + draft form.)
- **Field cleanup:** dropped `achievements` (work), `results` (projects), `leadership` (additional, value preserved in DB but hidden). Merged Volunteer INTO Work → one "Work / Volunteer" section (uses experience table; company field relabeled "Company / Organization"). Separate volunteer editor removed from profile + onboarding (volunteer table now unused but not dropped).
- **Light mode pink:** background #FFF8F2 → #FFF0F4 (soft blush) in tailwind.config + colors.ts + app.json splash. Pairs with dark-green primary.
- Verified tsc + ios export exit 0. No new deps → cnpull + r.

## Cover Nugget — onboarding access + length limits (2026-07-30, 22:12 UTC)
- **Onboarding was unreachable** (gate skips it once a profile exists). Added to Settings → "App": **Restart onboarding** (router.push('/onboarding')) and **Reset app data** (confirm → wipeDatabase → refreshOnboarded → replace('/')).
- **Length-limit system (per letter):**
  - Own counter `src/utils/textStats.ts` (countWords/countChars/countFor/withinLimit) — NOT LLM.
  - DB: cover_letters.limit_type/limit_value (SCHEMA_VERSION 2→3 + additive migration); model + toCoverLetter + updateCoverLetterLimit.
  - `LengthLimitControl` component: toggle + Words/Chars + number.
  - Editor: shows the control (edit mode), **always-on "N words · M chars" count** (both modes, our counter), and a **"Fit to N"** button when over. `fitToLength()` in coverLetter service loops the LLM (≤3 passes) to shorten, checking with OUR counter each time; user only sees the final in-limit result. Throws w/o LLM (Expo Go) → shows the "needs full app" message; counts still work.
  - Settings: removed the "keep letters under 300 words" AI-instruction example (length is per-letter now).
- Auto-shorten needs the real LLM (dev build); in Expo Go the counts + limit UI work but Fit shows the AI-unavailable message.
- Verified tsc + ios export exit 0. No new deps → cnpull + r.

## Cover Nugget — serif fix + hamburger emoji + drawer deco (2026-07-30, 22:xx UTC)
- **SERIF FONT REAL FIX:** earlier defaultProps.style approach was a no-op because NativeWind className always sets `style`, so defaultProps (only fills undefined props) never applied → user saw system font. Replaced with a render-level patch in app/_layout.tsx: monkey-patch RNText/RNTextInput `.render` to cloneElement with `style:[{fontFamily:SERIF}, el.props.style]` (fontFamily FIRST so className weight/size/color still win). SERIF = Georgia(iOS)/serif(Android). Now everything is serif.
- **🍔 hamburger:** drawer header headerLeft is now a Pressable Text "🍔" (toggleDrawer) instead of the 3-line icon. screenOptions made a function to access navigation.
- **Sidebar deco (subtle):** custom drawerContent (DrawerContentScrollView + DrawerItemList) with a 🦖🍗 mascot header + faint "🦖 · 🍗 · 🦖" footer (opacity-30). Not overboard.
- Verified tsc + ios & web export exit 0. No new deps → cnpull + r (may want full restart since native-ish drawer/header change; press r first).

## Cover Nugget — margins + preview name lock (2026-07-30, 22:25 UTC)
- Bumped side margins px-5→px-6 (ScreenContainer global + editor KeyboardAvoidingView) — cover-letter view felt cramped.
- Editor back chevron: bigger tap area (hitSlop 12, py-2 pr-4) + separation from the name.
- **Name editable only in edit mode:** editor title TextInput now `editable={mode==='edit'}` — can't edit the name from the read-only preview anymore.
- Verified tsc + ios export. No new deps → cnpull + r.
- OPEN: user's last line cut off ("after i click finish, the onboarding thing doesn…") — asked them to complete it.

## Cover Nugget — onboarding order, profile tabs, save buttons, scrape UX (2026-07-30, 23:12 UTC)
- **Onboarding starts on Personal** now: onboarding/index.tsx redirects to /onboarding/personal (welcome screen removed from flow); renumbered steps 1–7 (personal 1 … additional 7, total 7).
- **Profile tabs restyled** thin browser-underline style (active = accent underline + bold), short labels: Me / Skills / Work / Edu / Projects / Certs / More.
- **Explicit Save per tab:** RepeatableEditor editable mode no longer writes per keystroke — edits are local; a **Save** button persists all entries (update loop) → "Saved" alert. addBlank appends with new id (preserves other unsaved edits); remove filters locally. Skills tab got a Save button too; Personal/Additional already had Save.
- **Scrape UX in generate:** paste a link → auto-scrapes after ~900ms debounce → shows an indeterminate **loading bar** ("Reading the job link…"), a ✓/⚠️ status, and keeps **Generate disabled until scrape succeeds** (blocked links show the warning inline, Generate stays disabled). Generate uses the already-scraped text (no re-scrape).
- Verified tsc + ios export exit 0. No new deps → cnpull + r.

## Cover Nugget — editor toolbar redesign + fit progress (2026-07-30, 23:43 UTC)
- **Text stays big while highlighting:** removed the KeyboardAvoidingView squeeze in the editor (keyboard overlays instead of shrinking the letter). Selection persisted via `savedSel` (last non-empty range) so actions still work after the keyboard/highlight clears.
- **Categorized edit toolbar** (new src/components/EditToolbar.tsx): a "Selection | Whole letter" scope toggle + Length/Tone/Grammar/Custom categories that expand to detailed options (saves space). Selection→editSelection, Whole→editWhole, Custom→modal (branches on scope).
- **Shorten/regenerate progress:** fitToLength now takes {maxPasses,onPass,shouldCancel}; editor shows a FitBar (indeterminate) + "Shortening… pass X of N" + an ✕ cancel while running; all other actions greyed (busyAny). Cancel stops before the next pass.
- Verified tsc + ios export exit 0. No new deps → cnpull + r.
- JobSpy: answered — only the job-link/search lookup goes online (rest stays on-device). Hosting rec pending; noted free-host IPs get blocked by Indeed/LinkedIn.
